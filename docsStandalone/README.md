This folder contains the Latex source code for the "Cryptographic Primitives at Copper" specification.

To build the document you need `docker` installed on your machine. Then you can run the following command in the root of the krypton-primitives repository:

```bash
make docs
```