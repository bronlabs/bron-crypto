[package]
name = "test-sys"
version = "0.1.0"
links = "foo"
build = "build.rs"
edition = "2018"

[dependencies]
libloading = "0.7.4"
hex = "0.4.3"
tokio = {version = "1.14.0", features = ["full"]}

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
pkg-config = "0.3"
