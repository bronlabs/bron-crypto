name: CI

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: self-hosted
    env:
      GODEBUG: "cpu.avx2=off"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        run: make build test lint