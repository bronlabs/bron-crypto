info:
  name: krypton
  version: "0.1"

include:
  - file: /etc/serve/include.d/library.yml

build:
  - docker-image:
      no-push: true
      build-args ? {{ info.feature }}:
        "*": '--build-arg="TEST_ARGS=-short"'
        "": ""
