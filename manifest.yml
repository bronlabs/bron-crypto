info:
  name: krypton-primitives
  version: "0.1"

include:
  - file: /etc/serve/include.d/library.yml

build:
  - docker-image:
      images:
      - branch: "feature-INFSUP-1149-tests"
        Dockerfile: Dockerfile.nightly
        no-push: true