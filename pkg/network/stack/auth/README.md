# Auth
Defines the `Auth` layer of the protocol stack.
The responsibility of this layer is make any peer-to-peer exchange to be authenticated via authenticated encryption.
The final approach would be to use Signal-like protocol, for the PoC HPKE is being used.
