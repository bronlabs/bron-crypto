# FROST Aggregator

To get TS-SUF-4, the aggregator must recompute R. For simplicity, the aggregator always recomputes R irrespective of whether it acted as a present participant for a signature. A possible optimization is to cache R inside the aggregation parameters.
